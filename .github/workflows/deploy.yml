name: Deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Surge
        run: npx surge . connections-clone-clawdia.surge.sh --token ${{ secrets.SURGE_TOKEN }}
